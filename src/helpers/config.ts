
const config = {
    pages: [
        {
            name: 'home',
            path: '/',
        },
        {
            name: 'favoritos',
            path: 'favoritos',
        },
    ],
    userSettings: ['sair']
}

export default config;